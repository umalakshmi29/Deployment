{
  "name": "task4",
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "dependencies": {
    "@cap-js/audit-logging": "^1.2.0",
    "@cap-js/hana": "^2.7.0",
    "@sap-cloud-sdk/http-client": "^4.5.1",
    "@sap-cloud-sdk/resilience": "^4.5.1",
    "@sap/cds": "^9",
    "@sap/xsenv": "^6.1.0",
    "@sap/xssec": "^4.13.0",
    "express": "^4"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^2",
    "@sap/cds-dk": "^9"
  },
  "scripts": {
    "start": "cds-serve"
  },
  "private": true,
  "cds": {
    "requires": {
       "Location_API": {
       "kind": "rest",
       "credentials": {
           "destination": "Location_API"
      }
    },
      "auth": {
        "kind": "mocked"
      },
      "geo": {
        "kind": "odata",
        "credentials": {
          "destination": "Location_API"
        }
      }
    }
  }
}
